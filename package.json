{
  "name": "anishdebnath-portfolio",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @anishdebnath/web",
    "build": "npm run build --workspace @anishdebnath/web",
    "preview": "npm run preview --workspace @anishdebnath/web",
    "lint": "tsc --noEmit -p apps/web"
  },
  "devDependencies": {
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "concurrently": "^10.0.4"
  }
}
